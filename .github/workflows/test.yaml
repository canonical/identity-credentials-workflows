name: test actions
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: pull current repo
        uses: actions/checkout@v3
      - name: setup machine
        uses: ./.github/actions/prepare
